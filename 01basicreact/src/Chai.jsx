function Chai(){
    return (
        <>
        <h2>chai in react is ready.</h2>
        </>
    )
}

export default Chai;

// the functions which we export are called as components.
//learning --> wether working with react or vite the functions name or the components should always be capitalized 